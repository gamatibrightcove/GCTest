<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>DE_Ready__c</fullName>
    <externalId>false</externalId>
    <formula>OR(
AND(
NOT(CONTAINS(Email, &quot;@brightcove.com&quot;)),
NOT(ISBLANK( Country ))
),
AND( 
De_Offer_Type__c = &quot;Generic&quot;,
( DE_Days_From_Create__c ) &gt; 1 /*If after a day nothing happens then redistribute*/
)/*,end of the AND*/
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>DE - Ready</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
